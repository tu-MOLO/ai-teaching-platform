# AI Teaching Platform 审查报告

生成时间：2026-05-04 22:45:30（Asia/Shanghai）

## 结论

当前项目不适合直接投入使用或交付。

主要原因不是单点 bug，而是存在一组会直接影响上线/验收的阻断项：

1. 前端当前无法通过 TypeScript 构建，属于交付阻断。
2. 若按仓库默认 `docker-compose.yml` 启动，后端大概率会在启动前配置校验阶段直接失败。
3. 学生、教案、资源、权限/通知等模块存在多处前后端契合度问题，部分功能不可用，部分功能“看起来可用但行为与界面不一致”。
4. 部署编排引用了仓库中不存在的路径，说明交付链路未闭合。

以下按影响程度列出发现。

## 一、阻断交付 / 上线的高风险问题

### 1. 前端当前无法构建通过

已实际执行 `cmd /c npm run build`，构建失败。

已复现的错误包括但不限于：

- `frontend/src/pages/Students/Create.tsx:23` 传入了 `CreateStudentData` 中不存在的 `enrollment_date`
- `frontend/src/pages/Students/Edit.tsx:45` 读取 `StudentFormData` 中不存在的 `age`
- `frontend/src/pages/Students/Edit.tsx:78` 向 `Partial<StudentFormData>` 传入不存在的 `status`
- `frontend/src/pages/LessonPlanner/Create.tsx:60` 读取 `LessonPlanFormData` 中不存在的 `status`
- `frontend/src/pages/Courses/Edit.tsx:34` 课程状态类型不兼容
- `frontend/src/components/ResourceCenter/ResourceCard.tsx:36` 从 `services/resource` 导入不存在的 `Resource`
- `frontend/src/pages/ResourceCenter/index.tsx:16` 同类错误
- `frontend/src/services/permission.ts:58` / `:88` 返回类型与实际 `ListResponse<T>` 结构不兼容

影响：

- 当前前端产物无法稳定生成，项目无法形成可交付构建包。

### 2. 默认 Docker 编排与后端启动校验互相冲突

后端在模块导入时立即执行配置校验：

- `backend/app/main.py:28`

且在 `DEBUG=false` 时进入生产级严格校验：

- `backend/app/main.py:49`
- `backend/app/core/config_validator.py`

但 `docker-compose.yml` 默认给了明显会触发失败的占位/弱配置：

- `docker-compose.yml:80` `DEBUG: "false"`
- `docker-compose.yml:90` `SECRET_KEY: ${SECRET_KEY:-your-secret-key-here-change-in-production-min-32-characters}`
- `docker-compose.yml:97` `MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}`
- `docker-compose.yml:98` `MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}`

而 `config_validator` 明确会拒绝弱 `SECRET_KEY` 和弱 MinIO 凭据。

影响：

- 仓库给出的默认启动方式与代码中的启动前校验互相矛盾。
- 交付方若直接按文档或 compose 启动，后端大概率无法启动。

### 3. 部署编排引用了仓库中不存在的目录

`docker-compose.yml` 引用了以下路径：

- `docker-compose.yml:20` `./init-scripts`
- `docker-compose.yml:163` `./nginx/nginx.conf`
- `docker-compose.yml:164` `./nginx/ssl`

但当前仓库中：

- `init-scripts` 不存在
- `nginx` 目录不存在

影响：

- 数据库初始化链路不完整。
- 启用 `with-nginx` profile 时会直接失败。
- 说明部署产物和仓库内容未对齐，交付完整性不足。

### 4. 健康检查接口自身存在可疑实现，可能误报不健康

`/health` 中执行：

- `backend/app/main.py:202`
- `backend/app/main.py:203` `await result.scalar()`

这里 `scalar()` 的使用方式可疑，按常规 SQLAlchemy 结果对象通常不应再 `await`。

影响：

- 健康检查本应作为部署探针，但这里可能因为实现错误直接报错。
- 将进一步影响容器健康检查、反向代理探针和运维判断。

备注：

- 该点我未在运行态完整验证，因为本地后端依赖未装齐，但从实现看是明显风险。

## 二、会导致功能不可用或行为错误的前后端矛盾

### 5. 教案“创建并标记完成”功能实际不会按界面语义生效

前端创建页试图传入状态：

- `frontend/src/pages/LessonPlanner/Create.tsx:60`
- `frontend/src/pages/LessonPlanner/Create.tsx:61`

但：

- `frontend/src/types/forms.ts` 的 `LessonPlanFormData` 根本没有 `status`
- 后端 `LessonPlanCreate` 也没有 `status` 字段：`backend/app/schemas/lesson_plan.py:35`
- 服务层创建时强制写死为草稿：`backend/app/services/lesson_plan.py:42`

影响：

- 页面文案是“创建并标记完成”，实际后端始终创建为 `draft`。
- 属于典型“功能看似存在，实际无效”的交付风险。

### 6. 学生新增页日期格式与后端 schema 不匹配

学生表单使用 `DatePicker`，值类型是 `Dayjs`：

- `frontend/src/components/Students/StudentForm.tsx:7`
- `frontend/src/components/Students/StudentForm.tsx:84`
- `frontend/src/components/Students/StudentForm.tsx:91`

新增页提交时直接发送：

- `frontend/src/pages/Students/Create.tsx:22` `birth_date: values.birth_date?.toISOString()`
- `frontend/src/pages/Students/Create.tsx:23` `enrollment_date: values.enrollment_date?.toISOString()`

但后端字段是 `date`，且 validator 使用 `date.fromisoformat(v)`：

- `backend/app/schemas/student.py`

`toISOString()` 是完整时间戳，如 `2026-05-04T...Z`，不符合纯日期解析。

影响：

- 学生新增很可能直接 422 失败。
- 即使局部绕过，也存在时区导致日期偏移的风险。

### 7. 学生编辑页逻辑与表单结构严重脱节

编辑页使用了表单里不存在的字段：

- `frontend/src/pages/Students/Edit.tsx:45` 使用 `values.age`
- `frontend/src/pages/Students/Edit.tsx:78` 构造 `status`

而 `StudentFormData` 只有：

- `frontend/src/components/Students/StudentForm.tsx:7-13`

影响：

- 该页面已在构建阶段报错。
- 即使临时放宽 TS，运行时也会出现错误数据写入或字段丢失。

### 8. 路由存在重复/分叉实现，教案编辑页接入不一致

路由把教案编辑路由指向 `Create` 页面：

- `frontend/src/router/index.tsx:119`

同时项目中还存在单独的编辑页实现：

- `frontend/src/pages/LessonPlanner/Edit.tsx`

影响：

- 说明编辑链路存在双实现并存。
- 容易造成后续修复只改一处、另一处失效，增加交付和维护风险。

### 9. 课程默认状态前后端语义不一致

后端 schema 默认给 `active`：

- `backend/app/schemas/course.py:20`

但数据库模型默认是 `draft`：

- `backend/app/models/course.py:20`
- `backend/app/models/course.py:77`

影响：

- 课程创建时，接口层默认语义和数据库层默认语义不一致。
- 在“前端不显式传状态”或其他入口创建课程时，可能出现状态解释偏差。

### 10. 资源中心类型导出和消费不一致

资源服务文件中没有导出 `Resource` 类型，但多个页面/组件按命名导出引用：

- `frontend/src/services/resource.ts`
- `frontend/src/components/ResourceCenter/ResourceCard.tsx`
- `frontend/src/components/ResourceCenter/ResourcePreview.tsx`
- `frontend/src/pages/ResourceCenter/Detail.tsx`
- `frontend/src/pages/ResourceCenter/index.tsx`

影响：

- 当前已直接导致前端构建失败。
- 也反映资源模块类型边界未收敛。

### 11. 权限列表响应结构前后端不一致

后端通用列表响应用的是 `data`：

- `backend/app/schemas/base.py` `ListResponse`

前端权限服务却自定义成 `items`：

- `frontend/src/services/permission.ts:34`
- `frontend/src/services/permission.ts:42`

影响：

- 已直接触发 TypeScript 编译错误。
- 即使放宽类型，也容易在页面消费时读错字段。

### 12. 通知模块与其他列表接口结构风格不一致

通知列表接口返回 `items`：

- `backend/app/schemas/notification.py:72`
- `backend/app/api/v1/notifications.py:74`

前端通知服务也按 `items` 处理：

- `frontend/src/services/notification.ts:26`

这与项目其他大多数列表接口统一使用的 `data` 风格不同。

影响：

- 单点上未必是 bug，但会放大通用响应处理复杂度。
- 目前 `request.ts` 已经为不同返回风格加了特殊分支，说明响应契约没有统一。

## 三、数据层 / 迁移链路风险

### 13. Alembic 迁移历史不完整，无法证明能从零可靠建库

当前只有两条迁移：

- `backend/alembic/versions/a6187dbdd1d8_add_student_status_fields.py`
- `backend/alembic/versions/b234c5e6f789_fix_student_user_id_and_course_status.py`

首条迁移直接以 `students` 表为前提：

- `a6187dbdd1d8_add_student_status_fields.py:32`

但仓库中没有覆盖“初始建表”的迁移记录。

影响：

- 新环境从零执行 `alembic upgrade head` 的成功率存疑。
- 当前迁移更像“在既有数据库上打补丁”，不适合作为正式交付数据库版本链。

### 14. migration 容器会吞掉迁移失败

`docker-compose.yml` 中 migration 命令为：

- `alembic upgrade head || echo 'Migration completed or no migrations needed'`

影响：

- 真实迁移失败会被伪装成“已完成或无需迁移”。
- 会导致数据库结构问题被延迟暴露，极不利于交付验收。

## 四、无效功能、未完成功能、名不副实功能

### 15. 权限/通知存在明显“未真正完工”的管理能力

后端中有明确 TODO：

- `backend/app/api/v1/notifications.py` 创建通知、批量创建通知处缺管理员权限检查
- `backend/app/api/v1/auth.py` 注册后“发送验证邮件”仍是 TODO

影响：

- 一部分“后台管理/账号安全”能力仍停留在半成品状态。
- 若对外宣称已具备完整账号体系/通知管理，会与实际交付能力不符。

### 16. 仓库中存在未接入或重复页面，说明功能边界未收束

典型例子：

- `frontend/src/pages/LessonPlanner/Edit.tsx` 存在，但路由未使用
- 教案创建/编辑逻辑同时散落在 `Create.tsx` 与 `Edit.tsx`

影响：

- 容易出现“修了一个页面，另一个旧页面继续坏着”的问题。
- 这类冗余在交付后维护成本很高。

## 五、其他会影响交付质量的问题

### 17. README / 文档存在编码异常

根目录 `README.md` 打开后出现明显乱码。

影响：

- 降低项目可交接性。
- 启动、部署、验收说明的可信度下降。

### 18. 自动化验证链路当前不完整

本次验证中：

- `pytest -q` 未能运行，原因是当前环境缺少 `sqlalchemy`
- 前端构建已实际失败

影响：

- 说明仓库当前无法在“接手即验证”的状态下自证可用。
- 对交付项目而言，这是明显减分项。

## 风险分级汇总

### P0（阻断交付）

- 前端构建失败
- 默认 compose 与后端配置校验冲突，可能导致服务无法启动
- 部署编排引用缺失目录
- 迁移容器吞掉失败，数据库链路不可信

### P1（核心功能不可用/错误）

- 学生新增日期格式错误，可能直接无法创建
- 学生编辑页逻辑与表单结构脱节
- 教案“创建并标记完成”是无效功能
- 课程状态默认语义不一致
- 健康检查实现可疑，可能误报失败

### P2（交付体验和维护风险）

- 路由/页面双实现并存
- 通知/权限响应结构风格不统一
- 多处 TODO 和半成品管理能力
- 文档乱码

## 建议整改顺序

建议按以下顺序修复，而不是分散处理：

1. 先修复前端构建错误，确保 `npm run build` 通过。
2. 统一前后端契约：学生日期格式、教案状态、课程状态、权限/通知响应结构。
3. 补齐部署链路：修正 `docker-compose.yml` 默认配置，移除不存在的卷映射，禁止 migration 吞错。
4. 整理数据库版本链：补全初始迁移，验证从空库 `alembic upgrade head` 可成功。
5. 清理重复/未接入页面和未完成功能，收束功能边界后再准备交付。

## 本次审查方式

本次基于以下方式完成：

- 通读前后端与数据库相关目录结构
- 抽查启动入口、配置、路由、schema、service、迁移脚本
- 实际执行前端构建：`cmd /c npm run build`
- 尝试执行测试：`pytest -q`

未完成项：

- 未在完整依赖环境下跑通后端测试
- 未连接真实数据库做全链路联调

但仅凭当前已复现和已定位的问题，已经足以判断项目暂不适合直接投入使用或交付。
