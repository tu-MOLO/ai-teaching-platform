# 前后端与数据库衔接 Bug/矛盾 审查报告

> 审查日期：2026-05-05
> 审查范围：前端 API 服务层 ↔ 后端 API 端点 ↔ 数据库模型，三层衔接的字段映射、类型匹配、响应格式、验证规则、路由一致性

---

## 总览

共发现 **34 个衔接问题**，按类别分为：响应格式解析矛盾（8个）、字段映射不一致（10个）、验证规则矛盾（6个）、路由/接口不匹配（6个）、数据类型/枚举不一致（4个）。

---

## 一、响应格式解析矛盾（前端拦截器 vs 后端实际返回）

### 1.1 [Critical] 登录响应被拦截器错误解包

| 层级 | 结构 |
|------|------|
| 后端实际返回 | `{ token: { access_token, refresh_token, ... }, user: { id, username, ... } }` |
| 响应拦截器处理 | 检测到 `data` 字段存在 → 解包为 `response.data.data`（即 `{ token: {...}, user: {...} }`） |
| 前端 authService.login 期望 | `response.token.access_token` 和 `response.user` |

**问题链路**：
- 后端 `LoginResponse` 的结构是 `{ token: TokenData, user: UserAuthInfo }`，外层被 FastAPI 的 `response_model` 自动包装为 `{ data: { token: ..., user: ... } }`
- 拦截器检测到 `data` 字段存在且不是列表格式 → 执行 `return responseData.data`，即返回 `{ token: ..., user: ... }`
- 前端 `authService.login` 中 `response.token.access_token` 可以正常访问

**但是**：`refreshAccessToken`（request.ts:25）使用**原生 axios** 调用 `/auth/refresh`，后端返回 `TokenData` 被包装为 `{ data: { access_token, refresh_token, ... } }`。原生 axios 不经过拦截器，所以 `response.data` 是 `{ data: { access_token, ... } }`，而代码直接解构 `const { access_token, refresh_token } = response.data`，得到 `undefined`。

**影响**：Token 刷新静默失败，用户被强制登出。

**涉及文件**：
- 后端：[auth.py:188-246](backend/app/api/v1/auth.py#L188) — 返回 `TokenData`
- 前端：[request.ts:25-37](frontend/src/services/request.ts#L25) — 原生 axios 解构
- 前端：[auth.ts:90-92](frontend/src/services/auth.ts#L90) — 拦截器版 axios 解构

---

### 1.2 [High] 报告接口双重 DataResponse 包装

| 层级 | 结构 |
|------|------|
| 后端实际返回 | `{ data: { totalCourses: 5, ... } }`（DataResponse 包装） |
| 拦截器处理 | 检测到 `data` 字段 → 解包为 `{ totalCourses: 5, ... }` |
| 前端期望 | 直接访问 `dashboardData.totalCourses` |

**问题**：后端 `reports.py` 所有端点都使用 `DataResponse[dict]` 包装，拦截器会正确解包。但 `DataResponse` 的 `data` 字段类型是 `dict`（非结构化），前端 `DashboardReport` 接口定义了 `totalCourses`（camelCase），而后端 `ReportService` 返回的字典 key 是什么格式完全不可控。**如果后端返回 snake_case（如 `total_courses`），前端全部读到 `undefined`。**

**涉及文件**：
- 后端：[reports.py:22-43](backend/app/api/v1/reports.py#L22)
- 前端：[report.ts:84-87](frontend/src/services/report.ts#L84)
- 前端：[dashboard.ts:70-80](frontend/src/stores/dashboard.ts#L70)

---

### 1.3 [High] 教案接口 DataResponse 包装与前端 toListResponse 不匹配

| 层级 | 结构 |
|------|------|
| 后端列表返回 | `{ data: [...], total: 5, page: 1, page_size: 20, pages: 1 }`（ListResponse） |
| 后端详情返回 | `{ data: { id, title, ... } }`（DataResponse） |
| 拦截器处理列表 | 检测到 `total`/`page` 等元数据 → **不解包**，返回原始 `{ data, total, ... }` |
| 拦截器处理详情 | 检测到 `data` 字段且无列表元数据 → **解包**为 `{ id, title, ... }` |

**问题**：前端 `getLessonPlans` 使用 `toListResponse<LessonPlan>(response)`，此时 `response` 已经是 `{ data, total, ... }` 格式（拦截器保留了列表格式），`toListResponse` 只是 `as` 断言，功能正确。

但 `getLessonPlan`（详情）使用 `toItem<LessonPlan>(response)`，此时 `response` 已被拦截器解包为 `{ id, title, ... }`，`toItem` 也是 `as` 断言，功能正确。

**潜在风险**：如果后端某个端点从 `DataResponse` 改为直接返回对象（不包装），拦截器不会解包，前端会收到一个多余层级的结构。当前依赖后端**始终使用 DataResponse 包装**这一隐式约定。

**涉及文件**：
- 后端：[lesson_plans.py:28-47](backend/app/api/v1/lesson_plans.py#L28)
- 前端：[lessonPlan.ts:52-61](frontend/src/services/lessonPlan.ts#L52)

---

### 1.4 [High] 标签列表接口返回 ListResponse 但前端按 data 字段取值

| 层级 | 结构 |
|------|------|
| 后端返回 | `{ data: [TagListResponse, ...], total: 5, page: 1, ... }`（ListResponse） |
| 拦截器处理 | 检测到 `total`/`page` → 不解包，返回原始结构 |
| 前端代码 | `toListResponse<Tag>(response).data` |

**问题**：前端 `getTags` 使用 `toListResponse<Tag>(response).data` 取标签数组。由于拦截器保留了列表格式，`response` 是 `{ data: [...], total, ... }`，`toListResponse` 断言后 `.data` 可以正确获取数组。**但 `TagListResponse`（后端 Schema）只有 `id`、`name`、`color` 三个字段，而前端 `Tag` 接口还包含 `description`、`created_at`、`updated_at`。**这些额外字段在列表响应中不存在，前端读到 `undefined`。

**涉及文件**：
- 后端：[tag.py:42-48](backend/app/schemas/tag.py#L42) — TagListResponse 仅含 id/name/color
- 前端：[tag.ts:4-11](frontend/src/services/tag.ts#L4) — Tag 含 description/created_at/updated_at

---

### 1.5 [Medium] 通知列表使用 toItem 而非 toListResponse

| 层级 | 说明 |
|------|------|
| 后端返回 | `NotificationListResponse` — `{ data: [...], total, unread_count, page, ... }` |
| 拦截器处理 | 检测到 `unread_count` → 不解包 |
| 前端代码 | `toItem<NotificationListResponse>(response)` |

**问题**：`toItem` 语义上是"转换单个对象"，但此处传入的是列表响应对象。虽然 `as` 断言不影响运行时，但语义混淆。如果将来 `toItem` 添加运行时验证逻辑，此处会出错。

**涉及文件**：[notification.ts:60-62](frontend/src/services/notification.ts#L60)

---

### 1.6 [Medium] 月度趋势接口返回 DataResponse[list] 但前端用 toItem 解包

| 层级 | 结构 |
|------|------|
| 后端返回 | `{ data: [MonthlyTrendItem, ...] }`（DataResponse[list]） |
| 拦截器处理 | 检测到 `data` 字段且无列表元数据 → 解包为 `[MonthlyTrendItem, ...]` |
| 前端代码 | `toItem<MonthlyTrendItem[]>(response)` |

**问题**：拦截器解包后 `response` 是数组，`toItem` 断言为 `MonthlyTrendItem[]`，功能正确。但如果后端在 DataResponse 外层添加了列表元数据（如分页），拦截器行为会改变，前端会收到未解包的结构。

**涉及文件**：[report.ts:109-112](frontend/src/services/report.ts#L109)

---

### 1.7 [High] 通知类型枚举大小写不一致

| 层级 | 值 |
|------|-----|
| 数据库模型 NotificationType | `system`, `course`, `homework`, `exam`, `message`, `reminder`（**小写**） |
| 后端 Schema NotificationType | 同上，小写 |
| 前端 NotificationType 枚举 | `SYSTEM`, `COURSE`, `HOMEWORK`, `EXAM`, `MESSAGE`, `REMINDER`（**大写**） |

**问题**：前端发送 `type=SYSTEM` 给后端，后端 `NotificationType` 枚举值是小写 `system`，FastAPI 的 Query 参数解析会尝试匹配枚举值。由于 Python 枚举是 `str` 子类且值为小写，`SYSTEM` 无法匹配，后端会返回 422 验证错误。

**影响**：前端按通知类型筛选功能完全失效。

**涉及文件**：
- 后端：[notification.py:17-24](backend/app/models/notification.py#L17) — 小写枚举值
- 前端：[notification.ts:4-11](frontend/src/services/notification.ts#L4) — 大写枚举值

---

### 1.8 [High] 课程状态枚举值不匹配

| 层级 | 值 |
|------|-----|
| 数据库 CourseStatus | `active`, `inactive`, `draft` |
| 后端 CourseBase Schema | `status: str`（无枚举约束） |
| 前端 Course 接口 | `status: 'active' \| 'inactive' \| 'draft'` |

**问题**：后端 Schema 中 `status` 是纯字符串，没有枚举验证。前端虽然定义了联合类型，但发送任意字符串后端都不会拒绝。数据库层有枚举约束，但错误会在数据库层抛出，返回 500 而非 422。

**涉及文件**：
- 后端：[course.py:20](backend/app/schemas/course.py#L20) — `status: str`
- 后端：[course.py:16-20](backend/app/models/course.py#L16) — CourseStatus 枚举

---

## 二、字段映射不一致

### 2.1 [Critical] 教案模板字段名完全不同

| 数据库/后端模型字段 | 后端 Schema 字段 | 前端接口字段 |
|-------------------|----------------|------------|
| `structure` (Text, NOT NULL) | `structure` | `template_data` |
| `is_default` (Boolean) | `is_default` | **不存在** |

**问题**：
1. 前端 `LessonTemplate` 接口定义了 `template_data?: string`，但后端模型和 Schema 的字段名是 `structure`。前端发送 `{ template_data: "..." }` 时，后端 `LessonTemplateCreate` 不认识该字段，会被 Pydantic 忽略（非严格模式）或报错（严格模式）。而 `structure` 是 NOT NULL 字段，创建时会报数据库错误。
2. 前端缺少 `is_default` 字段，无法区分默认模板。
3. 前端多了 `subject`、`grade`、`duration` 字段，后端模型和 Schema 中完全不存在。

**影响**：教案模板的创建和展示功能完全无法工作。

**涉及文件**：
- 后端模型：[lesson_template.py:29-33](backend/app/models/lesson_template.py#L29) — `structure`
- 后端 Schema：[lesson_template.py:9-16](backend/app/schemas/lesson_template.py#L9) — `structure`
- 前端接口：[lessonTemplate.ts:7-17](frontend/src/services/lessonTemplate.ts#L7) — `template_data`

---

### 2.2 [High] 学生性别字段值不匹配

| 层级 | 值 |
|------|-----|
| 数据库 Gender 枚举 | `male`, `female`, `other` |
| 后端 Student Schema 输出 | `male`, `female`, `other`（经过 convert_gender_for_output） |
| 前端 StudentForm 默认值 | `'男'` |
| 前端 Student 接口 | `gender: string`（无约束） |

**问题**：StudentForm 的 `initialValues` 设置 `gender: '男'`，但后端存储和返回的值是 `'male'`。当 `ConfigurableSelect` 从 API 加载下拉选项时，选项的 `value` 是 `'male'`，与默认值 `'男'` 不匹配，导致表单初始状态显示异常（选中项为空或显示原始值 `'男'`）。

**涉及文件**：
- 后端：[student.py:14-18](backend/app/models/student.py#L14) — Gender 枚举
- 前端：[StudentForm.tsx:38-42](frontend/src/components/Students/StudentForm.tsx#L38) — 默认值 `'男'`

---

### 2.3 [High] Portfolio Schema 缺少 student_name/student_grade/student_class 字段

| 层级 | 字段 |
|------|------|
| 后端 PortfolioWithStudent Schema | `student_name`, `student_grade`, `student_class` |
| 前端 PortfolioItem 接口 | 无这些字段 |

**问题**：后端定义了 `PortfolioWithStudent` 响应模型，包含关联的学生姓名、年级、班级信息。但前端 `PortfolioItem` 类型不包含这些字段。如果后端列表接口返回 `PortfolioWithStudent`，前端无法访问这些关联信息，导致列表页无法显示学生姓名。

**涉及文件**：
- 后端：[portfolio.py:72-75](backend/app/schemas/portfolio.py#L72) — PortfolioWithStudent
- 前端：[portfolio.ts:1-15](frontend/src/types/portfolio.ts#L1) — PortfolioItem

---

### 2.4 [High] PortfolioUpdate Schema 缺少 student_id 但前端类型包含

| 层级 | 字段 |
|------|------|
| 后端 PortfolioUpdate | 无 `student_id` 字段 |
| 前端 PortfolioItemUpdate | `student_id?: string` |

**问题**：前端 `PortfolioItemUpdate` 包含 `student_id` 可选字段，但后端 `PortfolioUpdate` Schema 不包含该字段。如果前端发送 `{ student_id: "xxx" }`，Pydantic 会忽略该字段（非严格模式），学生 ID 不会被更新。这可能导致用户以为修改了关联学生，但实际没有生效。

**涉及文件**：
- 后端：[portfolio.py:39-59](backend/app/schemas/portfolio.py#L39)
- 前端：[portfolio.ts:30-41](frontend/src/types/portfolio.ts#L30)

---

### 2.5 [Medium] UserProfile 接口缺少 permissions 字段

| 层级 | 字段 |
|------|------|
| 后端 CurrentUserResponse | `permissions: list[str]` |
| 前端 UserProfile 接口 | 无 `permissions` 字段 |

**问题**：`/auth/me` 端点返回 `CurrentUserResponse`，包含 `permissions` 字段。前端 `UserProfile` 接口没有定义该字段，权限信息被丢弃。如果前端需要做权限控制（如路由守卫），无法获取用户权限列表。

**涉及文件**：
- 后端：[auth.py:97](backend/app/schemas/auth.py#L97) — permissions 字段
- 前端：[user.ts:3-18](frontend/src/services/user.ts#L3) — UserProfile 无 permissions

---

### 2.6 [Medium] UserStore 只存 4 个字段，后端 UserAuthInfo 有 7 个字段

| 层级 | 字段 |
|------|------|
| 后端 UserAuthInfo | `id`, `email`, `username`, `full_name`, `avatar_url`, `role`, `is_active` |
| 前端 UserStore.User | `id`, `username`, `email`, `role` |

**问题**：前端 UserStore 的 User 类型只有 4 个字段，缺少 `full_name`、`avatar_url`、`is_active`。登录时 `setUser(response.user)` 传入的是完整的 `UserAuthInfo` 对象，Zustand 会存储所有字段，但 TypeScript 类型声明只有 4 个。在严格模式下，访问 `user.full_name` 会报类型错误。

**涉及文件**：
- 后端：[auth.py:53-63](backend/app/schemas/auth.py#L53)
- 前端：[user.ts:3-8](frontend/src/stores/user.ts#L3)

---

### 2.7 [Medium] Tag 前端接口包含 description/created_at/updated_at，后端列表响应不含

| 层级 | 字段 |
|------|------|
| 后端 TagListResponse（列表用） | `id`, `name`, `color` |
| 后端 TagResponse（详情用） | `id`, `name`, `description`, `color`, `created_at`, `updated_at` |
| 前端 Tag 接口 | `id`, `name`, `description?`, `color?`, `created_at?`, `updated_at?` |

**问题**：前端 `getTags` 调用列表接口，后端返回 `TagListResponse`（仅 id/name/color）。前端 Tag 接口期望的 `description`、`created_at`、`updated_at` 在列表响应中不存在，值为 `undefined`。如果前端标签列表页需要显示描述或创建时间，会显示空白。

**涉及文件**：
- 后端：[tag.py:42-48](backend/app/schemas/tag.py#L42)
- 前端：[tag.ts:4-11](frontend/src/services/tag.ts#L4)

---

### 2.8 [Medium] ResourceCreate Schema 包含 tag_ids 但前端用 FormData 上传

| 层级 | 说明 |
|------|------|
| 后端 ResourceCreate | `{ name, description, tag_ids: List[str] }`（JSON Schema） |
| 前端上传方式 | `FormData`（multipart/form-data） |

**问题**：后端 `ResourceCreate` 是 Pydantic 模型，期望 JSON 输入。但文件上传必须使用 `multipart/form-data`。后端 `resources.py` 的 `create_resource` 端点接收 `UploadFile` 和 `ResourceCreate` 两个参数，FastAPI 会从 FormData 中解析。但 `tag_ids` 是 `List[str]` 类型，在 FormData 中需要特殊处理（如 `tag_ids[]=id1&tag_ids[]=id2`），前端直接 `FormData.append('tag_ids', JSON.stringify(ids))` 可能导致后端解析失败。

**涉及文件**：
- 后端：[resource.py:17](backend/app/schemas/resource.py#L17) — tag_ids: List[str]
- 前端：[resource.ts:29-36](frontend/src/services/resource.ts#L29)

---

### 2.9 [Low] Student 模型有 user_id 字段但前端接口不包含

| 层级 | 字段 |
|------|------|
| 数据库 Student 模型 | `user_id: Optional[str]`（外键关联 users.id） |
| 后端 Student Schema | 无 `user_id` 字段 |
| 前端 Student 接口 | 无 `user_id` 字段 |

**问题**：数据库模型有 `user_id` 外键，但后端 Schema 和前端接口都不暴露该字段。这意味着学生与用户的关联关系在前端完全不可见，无法实现"我的学生"功能。如果这是有意设计（学生不绑定特定用户），则 `user_id` 字段在数据库中是冗余的。

**涉及文件**：
- 后端模型：[student.py:83-89](backend/app/models/student.py#L83)
- 后端 Schema：[student.py:12-22](backend/app/schemas/student.py#L12)

---

### 2.10 [Low] Course 模型有 user_id 字段但前端接口不包含

| 层级 | 字段 |
|------|------|
| 数据库 Course 模型 | `user_id: Optional[str]`（外键关联 users.id） |
| 后端 CourseResponse Schema | 无 `user_id` 字段 |
| 前端 Course 接口 | `teacher: string`（教师姓名，非 ID） |

**问题**：与 2.9 类似，课程与用户的关联通过 `user_id` 外键实现，但前端只看到 `teacher`（字符串姓名）。`user_id` 和 `teacher` 之间没有关联约束，可能出现 `user_id` 指向用户 A 但 `teacher` 填写用户 B 姓名的情况。

**涉及文件**：
- 后端模型：[course.py:55-61](backend/app/models/course.py#L55)

---

## 三、验证规则矛盾

### 3.1 [Critical] 密码验证规则四处不统一

| 场景 | 前端验证 | 后端验证 |
|------|---------|---------|
| 注册 | `(?=.*[A-Za-z])(?=.*\d)` — 字母+数字，最少8位 | `min_length=8` — 仅长度，无复杂度 |
| 登录页重置密码 | `(?=.*[a-z])(?=.*[A-Z])(?=.*\d)` — 大小写+数字 | `min_length=8` — 仅长度 |
| Profile 修改密码 | `min: 8` — 仅长度 | `min_length=8` — 仅长度 |
| 后端 UserCreate | — | 大写+小写+数字 |

**矛盾**：
1. 前端注册允许 `Abc12345`（满足字母+数字），后端 UserCreate 要求大小写+数字，但注册用的是 RegisterRequest（无复杂度验证），所以 `abc12345` 也能注册成功
2. 前端重置密码要求大小写+数字，但后端 PasswordResetRequest 只要求 `min_length=8`
3. 用弱密码注册的用户，重置密码时必须用强密码，体验矛盾

**涉及文件**：
- 后端：[auth.py:32](backend/app/schemas/auth.py#L32) — RegisterRequest
- 后端：[user.py:33-45](backend/app/schemas/user.py#L33) — UserCreate
- 前端：[Register/index.tsx:51-62](frontend/src/pages/Register/index.tsx#L51)
- 前端：[Login/index.tsx:160-167](frontend/src/pages/Login/index.tsx#L160)

---

### 3.2 [High] 教案模板 structure 字段后端 NOT NULL 但前端可选

| 层级 | 约束 |
|------|------|
| 数据库 LessonTemplate.structure | `NOT NULL` |
| 后端 LessonTemplateCreate.structure | `...`（必填） |
| 前端 LessonTemplate.template_data | `template_data?: string`（可选） |

**问题**：前端 `template_data` 是可选字段，如果不传，后端 `structure` 是必填字段，会返回 422 验证错误。但由于字段名不同（`template_data` vs `structure`），前端发送的数据中根本没有 `structure` 字段，后端必定报错。

**涉及文件**：
- 后端：[lesson_template.py:15](backend/app/schemas/lesson_template.py#L15)
- 前端：[lessonTemplate.ts:14](frontend/src/services/lessonTemplate.ts#L14)

---

### 3.3 [Medium] 课程 status 后端无枚举验证，数据库有枚举约束

| 层级 | 约束 |
|------|------|
| 数据库 CourseStatus | `ENUM('active', 'inactive', 'draft')` |
| 后端 CourseBase.status | `str`（无枚举验证） |
| 前端 Course.status | `'active' \| 'inactive' \| 'draft'` |

**问题**：后端 Schema 的 `status` 字段是纯字符串，没有枚举约束。前端发送 `status: 'pending'` 时，后端不会在 Schema 层拒绝，而是到数据库层才报错（返回 500 而非 422）。

**涉及文件**：
- 后端：[course.py:20](backend/app/schemas/course.py#L20)

---

### 3.4 [Medium] Portfolio 评分后端 ge=0/le=100 但前端无范围验证

| 层级 | 约束 |
|------|------|
| 后端 PortfolioBase | `cognitive_score: Optional[int] = Field(None, ge=0, le=100)` |
| 前端 PortfolioItemCreate | `cognitive_score?: number`（无范围约束） |

**问题**：前端评分输入没有 0-100 的范围限制，用户可以输入负数或大于 100 的值，后端会返回 422。应在前端添加 `min: 0, max: 100` 的表单验证。

**涉及文件**：
- 后端：[portfolio.py:18-22](backend/app/schemas/portfolio.py#L18)
- 前端：[portfolio.ts:9-13](frontend/src/types/portfolio.ts#L9)

---

### 3.5 [Medium] DropdownOption group_key 后端必填但前端创建时可能漏传

| 层级 | 约束 |
|------|------|
| 后端 DropdownOptionCreate | `group_key: str = Field(..., min_length=1)` — 必填 |
| 前端 CreateDropdownOptionData | `group_key: string` — 必填 |

**问题**：虽然两端都声明为必填，但前端 Settings 页面创建下拉选项时，`group_key` 可能硬编码为当前编辑的分组，用户无法自定义。如果硬编码值与后端期望不匹配，会创建到错误的分组。

---

### 3.6 [Low] 教案 duration 后端必填但前端可能传 0

| 层级 | 约束 |
|------|------|
| 后端 LessonPlanBase | `duration: int = Field(...)` — 必填，无最小值 |
| 前端 LessonPlanFormData | `duration: number` — 必填，无最小值 |

**问题**：两端都没有 `ge=1` 约束，用户可以创建 `duration=0` 的教案，这在业务上不合理。

---

## 四、路由/接口不匹配

### 4.1 [Critical] 通知删除路由冲突

| 前端调用 | 后端路由 | 问题 |
|---------|---------|------|
| `DELETE /notifications/read/all` | `DELETE /notifications/{notification_id}` 先注册 | `read` 被当作 `notification_id` 解析 |

**问题**：后端 `delete_notification`（`DELETE /notifications/{notification_id}`）注册在 `delete_all_read`（`DELETE /notifications/read/all`）之前。FastAPI 按注册顺序匹配路由，所以 `DELETE /notifications/read/all` 中的 `read` 会被 `delete_notification` 捕获，`notification_id="read"`。然后 `NotificationService.delete` 查找 ID 为 `"read"` 的通知，找不到返回 False，抛出 404。

前端调用 `deleteAllRead` 时，实际执行的是删除单个通知的逻辑，永远返回 404。

**涉及文件**：
- 后端：[notifications.py:227-246](backend/app/api/v1/notifications.py#L227) — delete_notification 先注册
- 后端：[notifications.py:249-262](backend/app/api/v1/notifications.py#L249) — delete_all_read 后注册
- 前端：[notification.ts:103-106](frontend/src/services/notification.ts#L103)

---

### 4.2 [High] 教案月度统计路由被详情路由拦截

| 前端调用 | 后端路由 | 问题 |
|---------|---------|------|
| `GET /lesson-plans/stats/monthly` | `GET /lesson-plans/{plan_id}` 先注册 | `stats` 被当作 `plan_id` 解析 |

**问题**：后端 `get_lesson_plan`（`GET /lesson-plans/{plan_id}`）注册在 `get_monthly_stats`（`GET /lesson-plans/stats/monthly`）之前。FastAPI 按注册顺序匹配，`GET /lesson-plans/stats/monthly` 中的 `stats` 会被当作 `plan_id`，然后查找 ID 为 `"stats"` 的教案，找不到返回 404。

前端调用 `getMonthlyStats` 时，永远返回 404。

**涉及文件**：
- 后端：[lesson_plans.py:93-114](backend/app/api/v1/lesson_plans.py#L93) — get_lesson_plan 先注册
- 后端：[lesson_plans.py:262-297](backend/app/api/v1/lesson_plans.py#L262) — get_monthly_stats 后注册
- 前端：[lessonPlan.ts:151-162](frontend/src/services/lessonPlan.ts#L151)

---

### 4.3 [High] 标签列表路由末尾斜杠不一致

| 前端调用 | 后端路由 |
|---------|---------|
| `GET /tags` | `GET /tags/`（末尾有斜杠） |

**问题**：前端 `getTags` 调用 `api.get('/tags')`（无斜杠），后端注册的路由是 `@router.get("/")`（在 tags router 下，即 `/tags/`）。FastAPI 默认会自动重定向末尾斜杠，但在某些配置下可能返回 307 重定向而非 200。如果前端 axios 不自动跟随重定向，会请求失败。

**涉及文件**：
- 后端：[tags.py:22](backend/app/api/v1/tags.py#L22) — `@router.get("/")`
- 前端：[tag.ts:13-16](frontend/src/services/tag.ts#L13) — `api.get('/tags')`

---

### 4.4 [Medium] 前端密码重置缺少 admin_token 字段

| 前端发送 | 后端期望 |
|---------|---------|
| `{ username, new_password }` | `{ username, new_password, admin_token? }` |

**问题**：前端 `PasswordResetData` 只有 `username` 和 `new_password`，没有 `admin_token` 字段。后端 `PasswordResetRequest` 的 `admin_token` 是 `Optional`，但如果未提供则走 `else` 分支抛出 `AuthorizationException("密码重置需要管理员授权")`。所以前端密码重置功能**永远返回 403**。

**涉及文件**：
- 后端：[auth.py:74-78](backend/app/schemas/auth.py#L74) — admin_token: Optional
- 后端：[auth.py:358-362](backend/app/api/v1/auth.py#L358) — 未提供则拒绝
- 前端：[auth.ts:44-47](frontend/src/services/auth.ts#L44) — 无 admin_token

---

### 4.5 [Medium] 前端 updateUserProfile 多余的 GET 请求

| 前端流程 | 问题 |
|---------|------|
| 1. `GET /auth/me` 获取当前用户 ID | 多余请求 |
| 2. `PUT /users/{id}` 更新用户资料 | 竞态条件 |

**问题**：`updateUserProfile` 先调用 `GET /auth/me` 获取用户 ID，再调用 `PUT /users/{id}`。两次请求之间存在竞态条件（用户可能在两次请求之间被删除）。用户 ID 应从 `useUserStore` 或 JWT token 中直接获取，无需额外请求。

**涉及文件**：[user.ts:34-36](frontend/src/services/user.ts#L34)

---

### 4.6 [Low] 前端课程列表不传 status 参数但后端支持

| 前端 | 后端 |
|------|------|
| CourseQueryParams 包含 `status?: string` | courses.py 支持 `status` 查询参数 |

**问题**：前端虽然定义了 `status` 参数，但 Courses 页面实际未使用该参数进行筛选（筛选 UI 可能缺失或未连接）。

---

## 五、数据类型/枚举不一致

### 5.1 [High] UserRole 后端只有 TEACHER，前端/脚本期望 ADMIN

| 层级 | 值 |
|------|-----|
| 数据库 UserRole 枚举 | `teacher`（仅一个值） |
| 后端 Schema | `UserRole = TEACHER = "teacher"` |
| 前端 UserStore | `role: string`（无约束） |
| init_data.py 脚本 | `UserRole.ADMIN`（不存在） |
| 迁移脚本 | `normalize_legacy_admin_roles.py`（将 ADMIN 改为 TEACHER） |

**问题**：系统只有一个角色 `teacher`，但迁移脚本暗示曾经有 `admin` 角色。前端没有角色区分逻辑，所有用户都是教师。如果需要管理员功能（如密码重置、下拉选项管理），当前没有角色支持。

**涉及文件**：
- 后端：[user.py:15-17](backend/app/models/user.py#L15) — 只有 TEACHER
- 后端：[init_data.py:76-98](backend/scripts/init_data.py#L76) — 引用不存在的 ADMIN

---

### 5.2 [Medium] 日期格式前后端不一致

| 层级 | 格式 |
|------|------|
| 数据库 | `date` 对象（Python） |
| 后端 Schema 输出 | ISO 8601 字符串 `"2020-01-15"`（经过 convert_date_to_str） |
| 后端 Schema 输入 | 接受 ISO 字符串（经过 convert_str_to_date） |
| 前端 | `string` 类型，无格式约束 |

**问题**：前端 `birth_date` 和 `enrollment_date` 字段是纯字符串，没有格式验证。如果用户输入 `"2020年1月15日"` 这样的中文日期，后端 `date.fromisoformat()` 会抛出 ValueError，返回 422。

---

### 5.3 [Medium] 后端 AuditSchema 包含 is_deleted/deleted_at 但前端不处理

| 层级 | 字段 |
|------|------|
| 后端 AuditSchema | `created_at`, `updated_at`, `is_deleted`, `deleted_at` |
| 前端接口 | `created_at`, `updated_at`（无 is_deleted/deleted_at） |

**问题**：后端所有继承 `AuditSchema` 的响应模型都包含 `is_deleted` 和 `deleted_at` 字段，这些字段会出现在 API 响应中。前端接口定义不包含这些字段，但 JSON 数据中实际存在。虽然不影响功能（前端忽略多余字段），但可能造成困惑（如调试时看到 `is_deleted: false`）。

---

### 5.4 [Low] ID 类型前后端不一致

| 层级 | 类型 |
|------|------|
| 数据库 | UUID（String(36)） |
| 后端 Schema | `str` |
| 前端 | `string` |

**问题**：后端使用 `str` 表示 ID，前端也使用 `string`，类型一致。但后端没有验证 ID 是否为合法 UUID 格式，前端发送任意字符串作为 ID 时，后端不会在 Schema 层拒绝，而是到数据库查询时找不到记录返回 404。

---

## 问题严重程度汇总

| 严重程度 | 数量 | 编号 |
|---------|------|------|
| Critical | 4 | 1.1, 2.1, 3.1, 4.1 |
| High | 12 | 1.2, 1.4, 1.7, 1.8, 2.2, 2.3, 2.4, 3.2, 4.2, 4.3, 4.4, 5.1 |
| Medium | 13 | 1.5, 1.6, 2.5, 2.6, 2.7, 2.8, 2.9, 2.10, 3.3, 3.4, 3.5, 4.5, 5.2, 5.3 |
| Low | 5 | 1.3, 3.6, 4.6, 5.4, (其他) |
| **合计** | **34** | |

---

## 修复优先级

### 立即修复（Critical）

1. **修复 Token 刷新响应解构**（1.1）— `refreshAccessToken` 应处理 `{ data: { access_token, ... } }` 格式
2. **修复教案模板字段名**（2.1）— 前端 `template_data` 改为 `structure`，添加 `is_default`
3. **统一密码验证规则**（3.1）— 全链路统一为"大小写+数字，最少8位"
4. **修复通知删除路由冲突**（4.1）— 将 `delete_all_read` 路由注册移到 `delete_notification` 之前

### 尽快修复（High）

5. **修复教案月度统计路由冲突**（4.2）— 将 `get_monthly_stats` 路由注册移到 `get_lesson_plan` 之前
6. **修复通知类型枚举大小写**（1.7）— 前端枚举值改为小写
7. **修复学生性别默认值**（2.2）— 改为 `'male'` 或从 API 获取
8. **添加 Portfolio 关联学生字段**（2.3）— 前端 PortfolioItem 添加 student_name 等
9. **修复密码重置缺少 admin_token**（4.4）— 前端添加 admin_token 输入或后端修改验证逻辑
10. **修复报告接口字段名大小写**（1.2）— 确保后端返回 camelCase 或前端适配 snake_case
11. **修复标签列表路由斜杠**（4.3）— 统一末尾斜杠
