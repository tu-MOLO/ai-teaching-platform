# 最终统一决策

> 时间：2026-05-05 02:28:42
> 适用目标：教师单角色、本地可用、可交付版本

---

## 一句话结论

**不废，不全量重写，不换技术栈。**

只做一件事：

**把项目强行收缩成“教师单角色 + React 单体前端 + FastAPI 单体后端 + SQLite 本地数据库 + 本地文件存储”的版本。**

---

## 保留

这些内容保留，继续修，不推倒。

### 业务模块

- 学生管理
- 课程管理
- 教案管理
- 资源中心
- 成长档案
- 通知

### 技术栈

- React 前端
- FastAPI 后端
- 现有主体数据库表

### 数据库主体表

- `users`
- `students`
- `courses`
- `lesson_plans`
- `resources`
- `portfolios`
- `notifications`
- `dropdown_options`
- `tags`
- `lesson_templates`

---

## 删除

这些内容不再继续保留产品逻辑。

### 产品逻辑删除项

- 管理员角色产品逻辑
- 邮箱验证
- 邮件链路依赖的重置逻辑
- 无实际承接的权限展示
- 演示型入口
- “开发中”“敬请期待”“联系技术支持”这类空转入口

### 架构复杂度删除项

- PostgreSQL 作为当前正式交付路径
- Docker 作为当前正式交付前提
- 多角色体系
- 非必要的对象存储主路径

---

## 重写

这些不是“继续补”，而是直接重写或重搭。

### 必须重写的模块

- 认证与账户链路
  - 登录
  - 注册
  - 本地密码重置
  - 个人资料
  - token 刷新/登出

- 设置页
  - 学校信息
  - 下拉项管理
  - 本地持久化
  - 主题配置

- 报表/仪表盘
  - 只保留真实统计
  - 删除假图表、假趋势、无法落地展示

- 启动/初始化/脚本链路
  - `start.bat`
  - `start.sh`
  - `init_data.py`
  - `create_user.py`
  - 数据库初始化流程

---

## 数据库决策

### 不重写业务表

数据库**不推倒重做业务模型**。

### 只重整数据库策略

- SQLite 作为唯一正式交付数据库
- 统一空库初始化方式
- 统一测试库和正式库边界
- 修复迁移链与初始化脚本
- 清理历史遗留角色/字段兼容逻辑

---

## 架构决策

### 不换技术架构

不做这些事：

- 不换前端框架
- 不换后端框架
- 不上微服务
- 不上消息队列
- 不引入 Redis 作为交付前提

### 只做架构收缩

把当前项目收缩为：

- 单体前端
- 单体后端
- 单体本地数据库
- 单体本地存储
- 单角色教师产品

---

## 执行顺序

只按这个顺序做：

1. 清理不用的复杂度
2. 重写认证与设置链路
3. 固化 SQLite 本地运行
4. 修通学生/课程/教案/资源主流程
5. 重写报表页
6. 收尾文案、脚本、初始化、测试

---

## 禁止再做的事

为了避免继续混乱，后续不要再往回走：

- 不恢复管理员产品逻辑
- 不恢复邮箱验证
- 不同时维护 SQLite 和 PostgreSQL 两套交付路径
- 不再为“以后也许会用”保留复杂抽象
- 不再做无后端支撑的展示型功能

---

## 最终判断

这个项目不是“废了”，而是“范围失控了”。

正确处理方式不是推倒重来，而是：

**砍掉扩展幻想，收缩成一个能交付的本地教师单体系统。**

