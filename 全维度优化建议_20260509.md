# AI教学平台 - 全维度可交付性审查报告

> 审查日期：2026-05-09  
> 项目名称：ai-teaching-platform  
> 审查范围：全栈项目（后端 + 前端 + DevOps）

---

## 目录

- [1. 项目概况](#1-项目概况)
- [2. 严重安全漏洞](#2-严重安全漏洞)
- [3. 功能缺陷与潜在Bug](#3-功能缺陷与潜在bug)
- [4. 性能优化建议](#4-性能优化建议)
- [5. 代码质量与架构问题](#5-代码质量与架构问题)
- [6. 前端问题](#6-前端问题)
- [7. DevOps与部署问题](#7-devops与部署问题)
- [8. 可维护性评估](#8-可维护性评估)
- [9. 缺失功能与测试覆盖](#9-缺失功能与测试覆盖)
- [10. 优先级实施路线图](#10-优先级实施路线图)

---

## 1. 项目概况

### 1.1 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | FastAPI | 0.109.2 |
| ORM | SQLAlchemy (async) | 2.0.27 |
| 数据库 | PostgreSQL (生产) / SQLite (开发) | 15 / - |
| 对象存储 | MinIO (含本地降级) | latest |
| 缓存 | Redis | 7.x |
| 前端框架 | React + TypeScript | 18.2 / 5.2 |
| UI库 | Ant Design | 5.12 |
| 状态管理 | Zustand | 4.4 |
| 构建工具 | Vite | 5.0 |
| 容器编排 | Docker Compose | 3.8 |

### 1.2 功能模块

- 用户认证（JWT + 密保问题重置密码）
- 课程管理（CRUD + 筛选/分页）
- 学生管理（CRUD + 学习进度 + 成长档案导出PDF）
- 教案设计（模板库 + 创建/编辑/发布/归档）
- 资源中心（文件上传/下载/预览 + 标签管理）
- 成长档案（能力雷达图 + 时间线 + 评估）
- 主题定制（明暗模式 + 预设主题 + 自定义颜色）
- 可配置下拉选项
- 通知系统

### 1.3 整体评价

项目整体架构设计合理，采用了前后端分离、异步ORM、软删除、乐观锁、令牌版本控制等现代工程实践。异常处理体系完整（分级错误码 + 自定义异常类 + 全局异常处理器），配置验证机制完善。但在安全加固、性能优化、测试覆盖和生产环境部署方面仍有显著改进空间。

---

## 2. 严重安全漏洞（高优先级）

### 2.1 [SEC-001] 硬编码的 SECRET_KEY 泄露风险

**文件位置：**
- [config.py:L48](file:///d:/Projects/ai-teaching-platform/backend/app/core/config.py#L48)
- [docker-compose.yml:L89](file:///d:/Projects/ai-teaching-platform/docker-compose.yml#L89)
- [.env.docker:L24](file:///d:/Projects/ai-teaching-platform/.env.docker#L24)
- [.env.example:L24](file:///d:/Projects/ai-teaching-platform/backend/.env.example#L24)

**问题描述：**
JWT SECRET_KEY 在多个配置文件中硬编码了默认值，虽然通过环境变量可覆盖，但这些默认值已提交到代码仓库中。`.env.docker` 文件被 `.gitignore` 中的 `!.env.docker` 规则显式保留在版本控制中，意味着任何人克隆仓库后都能看到默认密钥。

**风险等级：** 高危

**修复方案：**
1. 从 `config.py` 中移除默认 SECRET_KEY 值，改为必需配置项（无默认值则启动失败）
2. 将 `.env.docker` 从 `.gitignore` 的排除列表中移除，改为 `.env.docker.example`
3. 在所有文档中强调生产环境必须通过环境变量注入强密钥

```python
# config.py - 修改后
SECRET_KEY: str = ""  # 必须通过环境变量设置

@field_validator("SECRET_KEY")
@classmethod
def validate_secret_key(cls, v: str) -> str:
    if not v or len(v) < 32:
        raise ValueError("SECRET_KEY must be at least 32 characters")
    return v
```

---

### 2.2 [SEC-002] Refresh Token 版本校验不一致

**文件位置：** [auth.py:L170-L216](file:///d:/Projects/ai-teaching-platform/backend/app/api/v1/auth.py#L170-L216)

**问题描述：**
`refresh_token` 端点中，先用 `verify_token()` 校验 refresh token 类型和过期时间，随后又用 `decode_token()` 重复解码来做 jti 版本校验。`verify_token()` 不校验 token_version，导致攻击者在 token 类型和过期时间校验通过后，即使 token 已被注销（token_version 递增），仍存在一个时间窗口可以成功刷新。

**修复方案：**
将 `verify_token` 函数与版本校验统一，或在 refresh 端点中只使用 `decode_token` 并手动做完整校验：

```python
@router.post("/refresh", response_model=TokenData)
async def refresh_token(refresh_data: RefreshTokenRequest, db: DBSession) -> TokenData:
    payload = decode_token(refresh_data.refresh_token)
    if not payload or payload.type != "refresh":
        raise AuthenticationException(message="无效的刷新令牌", error_code=ErrorCode.TOKEN_INVALID)
    if not payload.sub:
        raise AuthenticationException(message="无效的刷新令牌", error_code=ErrorCode.TOKEN_INVALID)
    if payload.exp and datetime.now(timezone.utc) > payload.exp:
        raise AuthenticationException(message="刷新令牌已过期", error_code=ErrorCode.TOKEN_EXPIRED)

    # 校验用户状态和 token 版本
    stmt = select(User).where(User.id == payload.sub, User.is_deleted == False)
    user = (await db.execute(stmt)).scalar_one_or_none()
    if not user or not user.is_active:
        raise AuthenticationException(message="用户不存在或已被禁用", error_code=ErrorCode.UNAUTHORIZED)
    if payload.jti and str(user.token_version) != payload.jti:
        raise AuthenticationException(message="刷新令牌已失效", error_code=ErrorCode.TOKEN_REVOKED)
    # ...继续生成新token
```

---

### 2.3 [SEC-003] 资源文件下载未校验用户所有权

**文件位置：** [resources.py:L282-L307](file:///d:/Projects/ai-teaching-platform/backend/app/api/v1/resources.py#L282-L307)

**问题描述：**
`get_resource_file` 端点虽然接收 `current_user_id`，但实际调用 `get_resource_file_content` 时传递了 `user_id` 参数。查看 [resource.py:L335](file:///d:/Projects/ai-teaching-platform/backend/app/services/resource.py#L335)，`get_resource_by_id` 在传了 `user_id` 时会添加 `Resource.user_id == user_id` 过滤条件。这个逻辑是正确的，但存在潜在问题：如果 `user_id` 参数为 `None` 或空字符串时，没有任何所有权校验。

**风险等级：** 中危

**修复方案：**
在 `get_resource_file_content` 中强制要求 `user_id` 参数且进行非空校验：

```python
@staticmethod
async def get_resource_file_content(db, resource_id, user_id):
    if not user_id:
        raise AuthorizationException("需要用户认证")
    # ... 其余逻辑
```

---

### 2.4 [SEC-004] 基于内存的限流器无法跨工作进程共享

**文件位置：** [rate_limiter.py:L22-L135](file:///d:/Projects/ai-teaching-platform/backend/app/core/rate_limiter.py#L22-L135)

**问题描述：**
`RateLimiter` 类使用 Python 内存字典存储请求计数。当 uvicorn 以多 worker 模式运行时（如 Dockerfile 中 `--workers 4`），每个 worker 有独立的限流器实例，导致限流策略被稀释（每个 worker 独立计数，实际限流上限变为 N 倍）。

**风险等级：** 中危

**修复方案：**
1. 短期方案：单 worker 部署 + Nginx upstream 做水平扩展，在 Nginx 层做限流
2. 长期方案：使用 Redis 实现分布式限流器：

```python
class RedisRateLimiter:
    def __init__(self, redis_client):
        self.redis = redis_client

    async def is_allowed(self, key: str, config_name: str) -> tuple:
        config = self._configs[config_name]
        now = time.time()
        window_key = f"ratelimit:{key}"
        # 使用 sorted set 实现滑动窗口
        async with self.redis.pipeline() as pipe:
            pipe.zremrangebyscore(window_key, 0, now - config.window)
            pipe.zcard(window_key)
            pipe.zadd(window_key, {str(now): now})
            pipe.expire(window_key, config.window + 1)
            _, count, _, _ = await pipe.execute()
        # ...
```

---

### 2.5 [SEC-005] DEBUG模式下CORS允许所有来源且携带凭据

**文件位置：** [main.py:L110-L119](file:///d:/Projects/ai-teaching-platform/backend/app/main.py#L110-L119)

**问题描述：**
DEBUG 模式下 CORS 配置为 `allow_origins=["*"]` 同时 `allow_credentials=True`，这在浏览器中可能导致安全问题和 CORS 预检失败（根据 Fetch 规范，`credentials: include` 时不能使用 `*` 通配符）。

**修复方案：**
DEBUG 模式下也应明确指定允许的来源列表：

```python
if settings.DEBUG:
    app.add_middleware(
        CORSMiddleware,
        allow_origins=["http://localhost:5173", "http://localhost:3000", "http://127.0.0.1:5173"],
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )
```

---

### 2.6 [SEC-006] 前端令牌存储在 localStorage

**文件位置：** [auth.ts:L16-L33](file:///d:/Projects/ai-teaching-platform/frontend/src/stores/auth.ts#L16-L33)

**问题描述：**
JWT token 和 refresh token 存储在 `localStorage` 中，这使得任何 XSS 攻击都能轻易窃取令牌。虽然项目使用了 React，但 Ant Design 的某些组件可能有已知的 XSS 漏洞链，且项目中未使用 CSP (Content-Security-Policy) 头来缓解 XSS。

**风险等级：** 中危

**修复方案：**
1. 使用 httpOnly cookie 存储 token（需要后端配合设置 cookie）
2. 在 Nginx 配置中添加 CSP 头
3. 如必须用 localStorage，实现 token 短期过期 + refresh token rotation

---

## 3. 功能缺陷与潜在Bug

### 3.1 [BUG-001] 资源文件URL返回API路径而非实际URL

**文件位置：** [resource.py:L291](file:///d:/Projects/ai-teaching-platform/backend/app/services/resource.py#L291)

**问题描述：**
`get_resource_file_url` 方法返回的是 API 路由路径 `/api/v1/resources/{id}/file` 而非实际的 MinIO 预签名 URL。这导致前端每次请求文件都需要经过后端代理，无法利用 MinIO/CDN 的直接分发优势。

**修复方案：**
根据存储后端类型返回合适的 URL：
- MinIO 模式：返回预签名 URL
- 本地存储模式：返回 API 路径（当前行为保持）

```python
@staticmethod
async def get_resource_file_url(resource, expires=timedelta(hours=1)):
    storage = get_storage()
    if isinstance(storage, MinIOStorage):
        return storage.get_file_url(resource.file_path, expires)
    return f"{settings.API_V1_STR}/resources/{resource.id}/file"
```

---

### 3.2 [BUG-002] 数据库会话双重提交问题

**文件位置：**
- [database.py:L85-L98](file:///d:/Projects/ai-teaching-platform/backend/app/core/database.py#L85-L98)
- [resource.py:L87](file:///d:/Projects/ai-teaching-platform/backend/app/services/resource.py#L87)

**问题描述：**
`get_async_session` 依赖生成器在 `yield` 返回后会执行 `session.commit()`。但 `ResourceService.create_resource` 方法在第87行也手动调用了 `await db.commit()`。虽然 SQLAlchemy 对重复提交有容错处理，但在异常路径中可能导致行为不一致：service 层 `rollback()` 后，会话生成器的 `finally` 块还会再次 `close()`。

**修复方案：**
统一事务管理策略：所有提交由依赖生成器统一处理，service 层不手动 commit/rollback（仅在需要提前获取 ID 时才 flush）。

---

### 3.3 [BUG-003] 资源计数查询中存在重复的筛选条件构建

**文件位置：** [resource.py:L111-L183](file:///d:/Projects/ai-teaching-platform/backend/app/services/resource.py#L111-L183)

**问题描述：**
`get_resources` 方法中，`query` 和 `count_query` 的筛选条件构建代码完全重复，且由于 `count_query` 使用了 `group_by`，在 `tag_ids` 存在时分页计数逻辑使用了 `subquery()` 的二次查询方式，存在潜在的计数偏差（当 tag_ids 为空但其他条件使得 group_by 生效时）。

**修复方案：**
抽取公共的筛选条件构建函数，统一 query 和 count_query 的构建逻辑：

```python
def _apply_resource_filters(query, params):
    if params.keyword:
        query = query.where(or_(
            Resource.name.ilike(f"%{params.keyword}%"),
            Resource.description.ilike(f"%{params.keyword}%")
        ))
    # ... 其他筛选条件
    return query
```

---

### 3.4 [BUG-004] 配置验证器的误报问题

**文件位置：** [config_validator.py:L28-L40](file:///d:/Projects/ai-teaching-platform/backend/app/core/config_validator.py#L28-L40)

**问题描述：**
`WEAK_SECRET_PATTERNS` 包含 `'secret'` 字符串，这意味着任何包含 "secret" 字样的密钥（如使用 `secrets.token_urlsafe(64)` 生成的密钥中包含 "secret"）都会被误判为弱密钥。同样，任何密钥包含 "test" 或 "admin" 都会被标记。

**修复方案：**
改为全词匹配或以 `^` 锚定模式：

```python
WEAK_SECRET_PATTERNS = [
    r'^password$', r'^secret$', r'^123456$',
    r'^qwerty$', r'^admin$', r'^changeme$',
    r'^default$', r'^test$'
]
```

---

### 3.5 [BUG-005] 令牌刷新器的竞态条件

**文件位置：** [request.ts:L11-L53](file:///d:/Projects/ai-teaching-platform/frontend/src/services/request.ts#L11-L53)

**问题描述：**
前端 token 刷新逻辑使用 `refreshPromise` 来避免并发刷新，但 `authExpiredHandled` 标志位是模块级全局变量，多个并发请求可能导致：第一个请求触发刷新并重置 `authExpiredHandled=false`，第二个请求也尝试刷新（因为 `refreshPromise` 尚未创建），导致不必要的刷新调用。

**修复方案：**
将 `authExpiredHandled` 合并到 `refreshPromise` 的状态管理中：

```typescript
let refreshPromise: Promise<string | null> | null = null

function startTokenRefresh(): Promise<string | null> {
  if (refreshPromise) return refreshPromise

  refreshPromise = (async () => {
    try {
      const { refreshToken, logout } = useAuthStore.getState()
      if (!refreshToken) return null
      // ... refresh logic
    } catch {
      return null
    }
  })()

  refreshPromise.finally(() => { refreshPromise = null })
  return refreshPromise
}
```

---

### 3.6 [BUG-006] 资源创建时重复查询数据库

**文件位置：** [resources.py:L95-L104](file:///d:/Projects/ai-teaching-platform/backend/app/api/v1/resources.py#L95-L104)

**问题描述：**
`create_resource` 端点中，`resource_service.create_resource()` 返回的 resource 对象已包含所有字段，但紧接着又调用 `resource_service.get_resource_by_id()` 再查一次数据库。这是不必要的消耗。

**修复方案：**
直接使用 `create_resource` 返回的对象：

```python
resource = await resource_service.create_resource(
    db=db, resource_data=resource_data,
    file_data=file.file, file_name=file.filename, user_id=current_user_id
)
file_url = await resource_service.get_resource_file_url(resource)
resource_dict = serialize_resource(resource, file_url=file_url)
return DataResponse(data=resource_dict)
```

---

## 4. 性能优化建议

### 4.1 [PERF-001] 资源查询中 ILIKE 模式匹配无法利用索引

**文件位置：** [resource.py:L117-L119](file:///d:/Projects/ai-teaching-platform/backend/app/services/resource.py#L117-L119)

**问题描述：**
`Resource.name.ilike(f"%{params.keyword}%")` 这种 `%keyword%` 模式无法使用普通 B-tree 索引，在数据量增大时性能衰减明显。

**修复方案：**
1. PostgreSQL 环境：使用 `pg_trgm` 扩展创建 GIN 索引：
```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE INDEX ix_resources_name_trgm ON resources USING gin (name gin_trgm_ops);
```
2. 为 `file_type` 字段添加普通索引

---

### 4.2 [PERF-002] 批量查询优化

**文件位置：** [resource.py:L159-L182](file:///d:/Projects/ai-teaching-platform/backend/app/services/resource.py#L159-L182)

**问题描述：**
资源列表查询中存在两次独立的数据库往返（count + data），在高并发场景下 count 查询可能返回与 data 查询不一致的结果（幻读问题）。

**修复方案：**
使用窗口函数在一次查询中同时获取数据和总数：

```sql
SELECT *, COUNT(*) OVER() as total_count
FROM resources WHERE is_deleted = false
LIMIT 20 OFFSET 0
```

SQLAlchemy 实现：
```python
from sqlalchemy import over

count_col = func.count().over().label("total_count")
query = select(Resource, count_col).where(Resource.is_deleted == False)
# ... filters and pagination
```

---

### 4.3 [PERF-003] 文件上传缺少请求体大小限制中间件

**文件位置：** [main.py](file:///d:/Projects/ai-teaching-platform/backend/app/main.py)

**问题描述：**
虽然 `settings.MAX_UPLOAD_SIZE` 定义了 100MB 上传限制，但这个限制只在 `create_resource` 端点的业务逻辑中手动检查。恶意用户可以发送超大请求体，在到达业务逻辑之前就耗尽服务器内存。

**修复方案：**
在应用层面添加请求体大小限制：

```python
from starlette.middleware.base import BaseHTTPMiddleware
from starlette.requests import Request

class MaxBodySizeMiddleware(BaseHTTPMiddleware):
    def __init__(self, app, max_size: int = 100 * 1024 * 1024):
        super().__init__(app)
        self.max_size = max_size

    async def dispatch(self, request: Request, call_next):
        if request.headers.get("content-length"):
            content_length = int(request.headers["content-length"])
            if content_length > self.max_size:
                return JSONResponse(
                    status_code=413,
                    content={"error": "FILE_TOO_LARGE", "message": "文件超过大小限制"}
                )
        return await call_next(request)
```

---

### 4.4 [PERF-004] Nginx gzip 缺少常用类型

**文件位置：** [nginx.conf:L9-L10](file:///d:/Projects/ai-teaching-platform/frontend/nginx.conf#L9-L10)

**问题描述：**
Nginx gzip 配置中未包含 `application/javascript` 的完整变体、`image/svg+xml` 等类型。

**修复方案：**
```nginx
gzip_types text/plain text/css text/xml text/javascript
           application/javascript application/x-javascript
           application/json application/xml application/xml+rss
           image/svg+xml font/ttf font/otf font/woff font/woff2;
```

---

### 4.5 [PERF-005] 资源列表每次都请求文件URL

**文件位置：** [resources.py:L159-L168](file:///d:/Projects/ai-teaching-platform/backend/app/api/v1/resources.py#L159-L168)

**问题描述：**
列表接口中，当前未为每个资源获取文件 URL（只序列化了基础信息），这是正确的设计。但详情接口中每次都调用 `get_resource_file_url`，如果使用 MinIO 会生成预签名 URL，建议加入短期缓存。

---

## 5. 代码质量与架构问题

### 5.1 [CODE-001] QueryOptimizer 的 Monkey-Patching 反模式

**文件位置：** [performance.py:L62-L68](file:///d:/Projects/ai-teaching-platform/backend/app/core/performance.py#L62-L68)

**问题描述：**
`QueryOptimizer.eager_load_relations` 装饰器通过临时替换 `db.get` 方法来实现预加载，这是典型的 Monkey-Patching 反模式。如果异步代码中有多个并发的 `await`，这种全局状态修改可能导致不可预期的行为。

**修复方案：**
废弃 `eager_load_relations` 装饰器。在调用方直接构建带 `options` 的 select 语句：

```python
# 替代方案：在调用处显式构建查询
stmt = select(Student).where(Student.id == student_id).options(
    selectinload(Student.portfolios),
    selectinload(Student.courses)
)
result = await db.execute(stmt)
student = result.scalar_one_or_none()
```

---

### 5.2 [CODE-002] 资源API端点中重复的异常处理模板

**文件位置：** [resources.py](file:///d:/Projects/ai-teaching-platform/backend/app/api/v1/resources.py)

**问题描述：**
四个资源端点（create/get/update/delete）都有相同的 `try/except` 异常处理模板，每个都独立 catch 特定异常类型再转换为 `InternalException`。这种重复代码降低了可维护性，且新增端点容易遗漏处理。

**修复方案：**
将资源操作的异常处理提取到装饰器或中间件中：

```python
def handle_resource_errors(func):
    @wraps(func)
    async def wrapper(*args, **kwargs):
        try:
            return await func(*args, **kwargs)
        except (BadRequestException, NotFoundException, AuthorizationException):
            raise
        except Exception as e:
            logger.error(f"Resource operation failed: {e}")
            raise InternalException("资源操作失败")
    return wrapper
```

---

### 5.3 [CODE-003] 混合同步/异步存储接口

**文件位置：** [storage.py](file:///d:/Projects/ai-teaching-platform/backend/app/services/storage.py)

**问题描述：**
`MinIOStorage` 和 `LocalFileStorage` 的所有方法都是同步的（直接文件 I/O / MinIO SDK 同步调用），但在异步上下文中被调用。对于大文件上传/下载，同步 I/O 会阻塞整个事件循环。

**修复方案：**
使用 `asyncio.to_thread()` 或 `run_in_executor` 包装同步 I/O 调用：

```python
import asyncio

async def upload_file_async(self, file_data, object_name, **kwargs):
    return await asyncio.to_thread(self.upload_file, file_data, object_name, **kwargs)
```

---

### 5.4 [CODE-004] is_allowed_file 函数逻辑过于复杂

**文件位置：** [storage.py:L679-L734](file:///d:/Projects/ai-teaching-platform/backend/app/services/storage.py#L679-L734)

**问题描述：**
`is_allowed_file` 函数混合了魔数校验、MIME 类型校验、扩展名校验三种逻辑，嵌套条件深达 4 层，且对 mp3/mp4 有特殊分支处理，可读性差。

**修复方案：**
拆分为独立的校验步骤：

```python
def is_allowed_file(filename: str, file_content: bytes = None) -> bool:
    ext = Path(filename).suffix.lower()
    if ext not in settings.ALLOWED_EXTENSIONS:
        return False
    if file_content is None:
        return True
    return _verify_magic_bytes(ext, file_content) and _verify_mime_type(ext, filename)
```

---

### 5.5 [CODE-005] alembic 迁移中部分索引仅支持 PostgreSQL

**文件位置：**
- [user.py:L168-L171](file:///d:/Projects/ai-teaching-platform/backend/app/models/user.py#L168-L171)
- [d456e7f8a901_add_partial_unique_indexes.py](file:///d:/Projects/ai-teaching-platform/backend/alembic/versions/d456e7f8a901_add_partial_unique_indexes.py)

**问题描述：**
User 模型的 `__table_args__` 使用了 `postgresql_where` 参数创建部分唯一索引，这在 SQLite 开发环境下会导致创建索引失败。虽然 SQLite 在开发模式可能不会触发此问题，但代码本身存在兼容性隐患。

**修复方案：**
在索引创建逻辑中添加数据库类型判断，或使用 Alembic 的 `@dialect` 装饰器区分不同数据库的迁移脚本。

---

## 6. 前端问题

### 6.1 [FE-001] auth store 的 initializeAuth 直接使用 axios

**文件位置：** [auth.ts:L34-L58](file:///d:/Projects/ai-teaching-platform/frontend/src/stores/auth.ts#L34-L58)

**问题描述：**
`initializeAuth` 方法使用原生 `axios` 而非项目中配置好的 `request` 实例。这意味着初始化阶段的请求不会经过拦截器（如统一的错误处理、baseURL 转换），导致行为与常规 API 调用不一致，且 token 过期后不会触发自动刷新逻辑。

**修复方案：**
使用统一的 `request` 实例替代原生 axios 调用。

---

### 6.2 [FE-002] v1 API 路径硬编码

**文件位置：** [request.ts:L7](file:///d:/Projects/ai-teaching-platform/frontend/src/services/request.ts#L7)

**问题描述：**
Base URL `/api/v1` 硬编码在前端配置中。如果未来需要 API 版本升级到 v2，需要修改此配置。建议通过环境变量注入。

**修复方案：**
```typescript
const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api/v1',
  timeout: 10000
})
```

---

### 6.3 [FE-003] 缺少统一的加载状态和空状态处理

**问题描述：**
多个页面组件（如 Students/index.tsx、Courses/index.tsx）中缺少全局加载骨架屏和空状态的统一处理。部分页面在数据为空时直接显示空白表格，用户体验不佳。

**修复方案：**
创建通用组件：
- `LoadingSkeleton` - 表格/卡片加载骨架屏
- `EmptyState` - 空状态提示（含引导操作按钮）

并在各列表页面统一使用。

---

### 6.4 [FE-004] 生产构建 sourcemap 已关闭

**文件位置：** [vite.config.ts:L83](file:///d:/Projects/ai-teaching-platform/frontend/vite.config.ts#L83)

**正面评价：** 生产构建中 `sourcemap: false` 是正确的安全实践，防止前端源码泄露。但需要注意在需要调试生产问题时，应有条件构建 sourcemap 上传到 Sentry 等错误追踪服务。

**建议：** 配置 Sentry 或其他错误监控服务的 sourcemap 上传。

---

## 7. DevOps与部署问题

### 7.1 [OPS-001] 数据库迁移服务使用 sleep 等待

**文件位置：** [docker-compose.yml:L138-L151](file:///d:/Projects/ai-teaching-platform/docker-compose.yml#L138-L151)

**问题描述：**
migration 服务使用 `sleep 10` 等待数据库就绪，这是一种不可靠的启动方式。如果数据库初始化需要超过 10 秒，迁移将失败。而 `depends_on.condition: service_healthy` 已经确保了 db 就绪后才启动 migration 容器。

**修复方案：**
移除 sleep，直接依赖健康检查结果：

```yaml
migration:
  # ...
  command: >
    sh -c "alembic upgrade head"
  depends_on:
    db:
      condition: service_healthy
```

---

### 7.2 [OPS-002] Nginx 缺少安全相关的头配置

**文件位置：** [nginx.conf](file:///d:/Projects/ai-teaching-platform/frontend/nginx.conf)

**问题描述：**
Nginx 配置中缺少 HSTS、CSP、Referrer-Policy 等安全头。虽然后端 FastAPI 中间件添加了部分安全头（`X-Content-Type-Options`、`X-Frame-Options`、`X-XSS-Protection`），但通过 Nginx 反向代理时，静态资源请求不经过后端安全中间件。

**修复方案：**
```nginx
add_header X-Content-Type-Options "nosniff" always;
add_header X-Frame-Options "DENY" always;
add_header X-XSS-Protection "1; mode=block" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self'" always;
```

---

### 7.3 [OPS-003] HTTPS/TLS 配置缺失

**文件位置：** [docker-compose.yml](file:///d:/Projects/ai-teaching-platform/docker-compose.yml) / [nginx.conf](file:///d:/Projects/ai-teaching-platform/frontend/nginx.conf)

**问题描述：**
两个 compose 文件中都没有 HTTPS 配置，Nginx 仅监听 80 端口。对于生产环境，明文 HTTP 传输会导致所有认证令牌和数据被中间人攻击截获。

**修复方案：**
1. 在 docker-compose.prod.yml 中添加 SSL 证书挂载和 443 端口映射
2. 配置 Nginx 的 SSL 块
3. 添加 HTTP 到 HTTPS 的重定向

---

### 7.4 [OPS-004] Redis 未设置密码认证

**文件位置：** [docker-compose.yml:L53-L68](file:///d:/Projects/ai-teaching-platform/docker-compose.yml#L53-L68)

**问题描述：**
Redis 服务启动时未配置 `requirepass`，任何人都可以无密码连接到 Redis（虽然在同一 Docker 网络中，但内网安全也应遵循最小权限原则）。

**修复方案：**
```yaml
redis:
  image: redis:7-alpine
  command: >
    redis-server
    --appendonly yes
    --maxmemory 256mb
    --maxmemory-policy allkeys-lru
    --requirepass ${REDIS_PASSWORD:-RedisSecure@2026}
```

同时更新 REDIS_URL 格式为 `redis://:password@redis:6379/0`。

---

### 7.5 [OPS-005] Docker Compose version 字段已弃用

**文件位置：** [docker-compose.yml:L6](file:///d:/Projects/ai-teaching-platform/docker-compose.yml#L6)

**问题描述：**
`version: '3.8'` 在较新版本的 Docker Compose (v2.x) 中已被弃用，虽然仍能正常工作，但会在启动时产生警告信息。

**修复方案：**
移除 `version` 字段（Docker Compose v2 会自动识别格式）。

---

## 8. 可维护性评估

### 8.1 优势

| 评估维度 | 评分 | 说明 |
|----------|------|------|
| 代码规范 | 85/100 | 命名规范、类型注解完整、文档字符串覆盖良好 |
| 异常处理 | 90/100 | 分级错误码体系 + 自定义异常类 + 全局处理器，设计优秀 |
| 配置管理 | 85/100 | pydantic-settings + 启动时验证 + 环境区分，较完善 |
| 数据库设计 | 80/100 | 软删除 + 乐观锁 + UUID主键 + 部分唯一索引，设计合理 |
| API设计 | 78/100 | RESTful 风格 + 统一响应格式，但部分端点双注册(带/不带斜杠) |

### 8.2 改进建议

#### 8.2.1 API端点双注册问题

**文件位置：** [students.py:L39-L40](file:///d:/Projects/ai-teaching-platform/backend/app/api/v1/students.py#L39-L40)

每个资源端点同时注册了带尾部斜杠和不带的版本（如 `@router.post("/", ...)` + `@router.post("", ...)`），其中一个标记了 `include_in_schema=False` 以隐藏。这是为了解决 FastAPI 路由匹配问题，但增加了路由表复杂性。

**建议：** 统一使用不带尾部斜杠的路径，在应用层面配置 `redirect_slashes=False`。

---

#### 8.2.2 缺少类型安全的服务层依赖注入

当前服务类使用静态方法 + 全局单例模式（`get_resource_service()`），这种方式虽然简单但没有利用 FastAPI 的依赖注入系统。

**建议：** 将服务类设计为可注入的依赖，便于单元测试时 mock：

```python
async def get_resource_service(db: AsyncSession = Depends(get_async_session)) -> ResourceService:
    return ResourceService(db)
```

---

#### 8.2.3 缺少结构化日志上下文

日志记录缺少统一的请求追踪上下文（如 request_id、user_id），在排查问题时难以关联同一请求的多个日志条目。

**建议：** 在日志中间件中生成 request_id 并通过 `logging.LogRecord` 的 `extra` 字段传递上下文。

---

## 9. 缺失功能与测试覆盖

### 9.1 测试文件缺失

**严重程度：** 高

虽然 `requirements.txt` 中包含了 pytest、pytest-asyncio、pytest-cov、factory-boy 等测试依赖，但项目中未找到任何实际测试文件（`backend/tests/` 目录不存在）。README 中虽然有测试部分的说明，但无法实际执行测试。

**影响：** 无法验证核心业务逻辑的正确性，重构风险高。

**建议：**
1. 至少覆盖以下模块的单元测试：
   - `auth.py` - 登录/注册/Token刷新/密码重置
   - `security.py` - Token 编解码/密码哈希
   - `resource.py` - 资源 CRUD
2. 集成测试覆盖关键 API 端点
3. 设置 CI 流水线自动运行测试并检查覆盖率

---

### 9.2 前端API类型生成缺失

**建议：** 使用 OpenAPI 规范自动生成前端 TypeScript 类型定义（如 openapi-typescript），避免前后端类型不一致。

---

### 9.3 审计日志未集成到业务操作中

**文件位置：** [audit_log.py](file:///d:/Projects/ai-teaching-platform/backend/app/models/audit_log.py)

审计日志模型已定义，但在实际的 API 端点（如资源删除、用户密码修改）中未记录审计日志。建议在关键操作中集成审计日志记录。

---

### 9.4 关键技术债务汇总

| 问题 | 位置 | 建议 |
|------|------|------|
| `QueryOptimizer.eager_load_relations` Monkey-patch | performance.py:L62-68 | 废弃，改用显式 select options |
| 会话双重提交 | database.py + services | 统一事务管理策略 |
| 限流器内存存储 | rate_limiter.py | 迁移到 Redis |
| `is_allowed_file` 复杂度过高 | storage.py:L694-734 | 拆分为独立校验函数 |
| API 端点异常处理模板重复 | resources.py | 抽取装饰器 |

---

## 10. 优先级实施路线图

### 第一阶段：安全修复（1-2周，发布前必须完成）

| 优先级 | 编号 | 问题 | 预计工时 |
|--------|------|------|----------|
| P0 | SEC-001 | 移除硬编码 SECRET_KEY，变更 .gitignore | 2h |
| P0 | SEC-002 | 修复 Refresh Token 版本校验不一致 | 3h |
| P0 | SEC-003 | 资源文件访问权限校验加固 | 1h |
| P0 | OPS-003 | 配置 HTTPS/TLS | 4h |
| P1 | SEC-005 | 修复 DEBUG 模式 CORS 配置 | 0.5h |
| P1 | SEC-006 | 令牌存储安全加固（httpOnly cookie） | 6h |
| P1 | OPS-002 | Nginx 安全头配置 | 1h |

### 第二阶段：Bug修复与性能优化（2-3周）

| 优先级 | 编号 | 问题 | 预计工时 |
|--------|------|------|----------|
| P1 | BUG-001 | 修复资源文件URL返回逻辑 | 2h |
| P1 | BUG-002 | 统一数据库会话事务管理 | 4h |
| P1 | BUG-005 | 修复前端令牌刷新竞态条件 | 2h |
| P2 | BUG-003 | 重构资源查询筛选条件构建 | 3h |
| P2 | PERF-001 | 添加 pg_trgm 索引支持 | 2h |
| P2 | PERF-003 | 添加请求体大小限制中间件 | 1h |
| P2 | SEC-004 | 实现 Redis 分布式限流器 | 8h |
| P3 | PERF-004 | Nginx gzip 类型扩展 | 0.5h |

### 第三阶段：代码质量与可维护性（3-4周）

| 优先级 | 编号 | 问题 | 预计工时 |
|--------|------|------|----------|
| P2 | CODE-002 | 抽取资源API异常处理装饰器 | 2h |
| P2 | CODE-004 | 重构 is_allowed_file 函数 | 2h |
| P2 | FE-001 | auth store 使用统一 request 实例 | 1h |
| P3 | CODE-001 | 废弃 QueryOptimizer Monkey-patch | 3h |
| P3 | CODE-003 | 存储层异步化包装 | 4h |
| P3 | OPS-001 | 优化 migration 服务启动策略 | 0.5h |
| P3 | FE-003 | 添加统一加载/空状态组件 | 4h |

### 第四阶段：测试与文档（持续进行）

| 优先级 | 内容 | 预计工时 |
|--------|------|----------|
| P1 | 编写核心模块单元测试（auth/security/resource） | 16h |
| P1 | 编写 API 集成测试 | 8h |
| P2 | 集成审计日志到关键业务操作 | 4h |
| P2 | 配置 CI 流水线 | 4h |
| P3 | OpenAPI -> TypeScript 类型自动生成 | 2h |
| P3 | 补充部署文档与运维手册 | 4h |

---

## 附录：快速检查清单

交付前必须确认以下项目：

- [ ] SECRET_KEY 已通过环境变量注入（非硬编码）
- [ ] DEBUG = false
- [ ] HTTPS 已配置
- [ ] 数据库密码为非默认值
- [ ] MinIO 密钥为非默认值
- [ ] Redis 已配置密码认证
- [ ] CORS 来源已限定为实际域名
- [ ] 所有 API 端点的权限校验已确认
- [ ] 核心业务流程的测试用例已通过
- [ ] 健康检查端点正常响应
- [ ] 日志级别设置为 INFO 或 WARNING

---

*本报告由 AI 辅助生成，基于对项目完整代码库的静态分析。建议结合动态测试和人工代码审查做出最终交付决策。*